const guards = [
    "Hank the tank",
    "Thomas the tank engine",
    "CEPUS"
];

export function addGuard(guard) {
    guards.push(guard);
    return guards;
}

// export default guards;

/* export default {
    guards,
    addGuard
}; */
